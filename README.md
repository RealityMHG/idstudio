# i-D Spotlight Clone (starter)

Scaffolded Vite + React single-page app to serve as a lightweight starting point for your cloning project.

Run locally:

```bash
npm install
npm run dev
```

Files to edit:
- `src/App.jsx`
- `src/components/*`
